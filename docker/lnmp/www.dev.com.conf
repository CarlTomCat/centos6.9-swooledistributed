server {
    listen 80;
    server_name www.dev.com;
    error_log /data/nginxlogs/www.dev.com.error.log error;
    index index.html index.htm index.php;
    root /data/wwwroot/www;

    include enable-php.conf;

    location /nginx_status {
        stub_status on;
        access_log off;
    }

    location ~ .*\.(gif|jpg|jpeg|png|bmp|swf)$ {
        expires 30d;
    }

    location ~ .*\.(js|css)?$ {
        expires 12h;
    }

    access_log /data/nginxlogs/www.dev.com.access.log access;
}